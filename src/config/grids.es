/**
 * Config for grids
 */
export default {
  /**
   * Defaults of field config objects
   * @type {Object}
   */
  defaults: {
    /**
     * Defaults by field name
     * @type {Object}
     */
    fields: {
      uri: {
        discard: true
      }
    },
    /**
     * Defaults by model
     * @type {Object}
     */
    models: {
    }
  }
}
